
{% extends 'base.html.twig' %}
{% block title %}{{ torneo.id ? 'Editar Torneo' : 'Crear Nuevo Torneo' }}{% endblock title %}
{% block body %}
    <h2>{{ torneo.id ? 'Editar Torneo' : 'Crear Nuevo Torneo' }}</h2>
            <hr>
            {{ form_start(formulario, {'attr': {'id': 'formTorneo'}}) }}
            {{ form_end(formulario) }}
            <a href="{{ path('equipo_guardar') }}" id="btn-add-equipo">Añadir equipo</a>
            <a href="{{ path('torneo',{'id': torneo.id}) }}">Cancelar</a>
            <div id="equipos"></div>
{% endblock %}

